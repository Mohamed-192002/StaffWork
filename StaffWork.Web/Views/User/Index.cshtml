@model Tuple<IEnumerable<UserViewModel>, UserFormViewModel>
@inject Microsoft.AspNetCore.Antiforgery.IAntiforgery antiforgery

@{
    ViewData["Title"] = "المستخدمين";
}
@section Styles
{
    <link rel="stylesheet" href="~/assets/plugins/datatables/datatables.bundle.css" />
}
<partial name="_Form" model="@Model.Item2" />
<div class="card shadow-sm">
    <partial name="_DataTablesCardHeader" />
    <div class="card-body pt-0">
        <div class="table-responsive">
            <table class="table table-row-dashed fs-2 gy-7 js-datatables" data-document-title="Users">
                <thead>
                    <tr class="fw-bold">
                        <th style="text-align:start">اسم المستخدم</th>
                        <th style="text-align:center">كلمه المرور</th>
                        <th style="text-align:center">الاسم بالكامل</th>
                        <th style="text-align:center">القسم</th>
                        <th style="text-align:center">الصلاحيه</th>
                        <th style="text-align:center">الحاله</th>
                        <th class="js-no-export" style="text-align:end">تعديل</th>
                        <th class="js-no-export" style="text-align:end">تغير الحاله</th>
                        <th class="js-no-export" style="text-align:end">حذف</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var type in @Model.Item1)
                    {
                        <partial name="_UserRow" model="@type" />
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<input type="hidden" name="__RequestVerificationToken" value="@antiforgery.GetAndStoreTokens(Context).RequestToken" />

@section Plugins
{
    <script src="~/assets/plugins/datatables/datatables.bundle.js"></script>
}

@section Scripts
{
    <partial name="_ValidationScriptsPartial" />
}